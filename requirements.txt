# Runtime dependencies for the AlertSage Streamlit UI.
# Trimmed for fast cold-start on Streamlit Community Cloud.
# For local dev, tests, docs, and notebooks: pip install -r requirements-dev.txt

# Web UI
streamlit>=1.39
streamlit-ace>=0.1.1

# Data and visualization
pandas>=2.0
numpy>=1.24,<3
plotly>=5.22

# ML inference. transformers is pinned below 5.0 because 5.x ships 80+
# image-processor submodules that lazy-import torchvision; on hosts
# without torchvision (we don't need it for text embeddings) every
# Streamlit module-path probe triggers a cascade of ModuleNotFoundError
# stack traces in the deploy log. Pinning to 4.x avoids the cascade
# without losing any functionality we use.
scikit-learn>=1.3
sentence-transformers>=3.0,<5
transformers>=4.40,<5
joblib>=1.3

# HTTP and console formatting
requests>=2.31
rich>=13

# LLM provider SDKs (all optional at runtime; only loaded when the user
# selects the matching provider in the sidebar).
openai>=1.50
anthropic>=0.40
